---
import MapCompositionPreview from './MapCompositionPreview.astro'

import type { Map } from '../domain/types'

interface Props {
  map: Map
  loading?: 'eager' | 'lazy'
}

const { map, loading = 'lazy' } = Astro.props
---

<a
  href={`/${map.slug}`}
  class="group border-secondary-sand bg-charcoal relative block h-full max-h-48 overflow-hidden rounded-xl border md:max-h-64"
  data-testid="map-card"
>
  <h2
    class="text-canvas absolute right-0 bottom-4 left-0 z-10 mx-auto block px-4 text-center text-3xl font-bold md:text-4xl"
    transition:name={`map-title-${map.slug}`}
  >
    {map.title}
  </h2>
  <MapCompositionPreview
    map={map}
    loading={loading}
    class="h-full w-full"
    imageClass="h-full w-full object-cover transition-transform duration-600 group-hover:scale-140"
    widths={[360, 540, 720, 960]}
    sizes="(min-width: 768px) 33vw, 92vw"
  />
</a>
