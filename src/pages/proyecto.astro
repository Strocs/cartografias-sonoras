---
import Layout from '../layouts/Layout.astro'
import { siteContent } from '../shared/data/site-content'

const { project } = siteContent
---

<Layout title={project.title} description={project.description}>
  <main class="flex-1 p-6 md:p-8">
    <h1 class="text-primary-navy text-3xl font-bold">{project.heading}</h1>
    <p class="text-primary-brown mt-2 max-w-2xl">{project.description}</p>
    <div class="text-primary-brown mt-6 max-w-3xl space-y-4">
      {project.body.map((paragraph) => <p>{paragraph}</p>)}
    </div>
  </main>
</Layout>
